# Git
.git
.gitignore

# Python
__pycache__
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env

# Node
node_modules/
frontend/node_modules/
frontend/dist/

# Logs & DB (Optional, usually we don't want to bake DB into image but want it in volume)
# We keep DB in context if we want to seed, but usually ignored
# logs/
# oraculo.db
